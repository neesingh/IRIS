function mode=index2mode(order,frequency)% index2mode  Convert double-index to single mode # for Zernike fns.%% Use:  mode=index2mode(order,frequency)%% input args:  	order (or degree) = integer power of radius variable%				frequency = integer multiple of angular variable%% LNT 4-Mar-99% LNT 3-Mar-00.  Rework the equations to match new ordering of modes incorporated%	in VSIA-2000 standard.  However, for compatibility with lab software, keep the%	old convention of piston=mode 1.  Just subtract 1 to convert mode to VSIA standard.%% See also: MODE2INDEX% error checkingif any(rem(order,1))		error('first argument must be an integer');endif any(order<0)		error('first argument must not be negative');endif any(rem(frequency,1))		error('second argument must be an integer');endmode = (order.*(order+2)+frequency)/2 +1;	% Jim Schwiegerling's equation% old method used in Thibos lab prior to 3-Mar-00% m = (order - frequency)/2;			%Malacara eqn. 13.21% mode = order.*(order+1)/2 + m + 1;	%Malacara eqn. 13.25% end of function index2mode