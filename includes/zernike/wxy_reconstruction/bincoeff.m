function out = bincoeff(n,r)%BINCOEFF  Return the binary coefficient N!/((N-R)!R!).%% Use: ans = bincoeff(n,r)%% Input arguments: integers n, r are usually written {N/R} and result is usually%    interpreted as N things taken R at a time.%% Method: brute force. Should be modified to implement ln(gamma) method for large n.%%Requires routine FACTORIAL %%LNT. 6-Mar-98.%error checking on input argumentif nargin < 2,     error('Requires two input arguments');endif any((imag(n) ~= 0) | (n < 0))      error('Input arguments must be real and nonnegative.')endif prod(size(n)) ~= 1       error('Input argument must be a scalar.')endif n ~= round(n)      error('Input argument must be an integer.')endif any((imag(r) ~= 0) | (r < 0))      error('Input arguments must be real and nonnegative.')endif prod(size(r)) ~= 1       error('Input argument must be a scalar.')endif r ~= round(r)      error('Input argument must be an integer.')endif r > n      error('Second argument cannot be greater than first argument.')end% do the calculationout = factorial(n)/(factorial(n-r)*factorial(r));